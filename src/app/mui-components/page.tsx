import { MuiSelect } from "@/components/MuiSelect";
import { Box } from "@mui/material";

const MuiComponents = () => {
  return (
    <Box flexGrow={1}>
      <MuiSelect />
    </Box>
  );
};

export default MuiComponents;
